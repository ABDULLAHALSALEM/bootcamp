[project]
name = "csv-profiler"
version = "0.1.0"
description = "A Python tool to profile CSV files and generate JSON and Markdown reports"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ipykernel>=7.1.0",
]